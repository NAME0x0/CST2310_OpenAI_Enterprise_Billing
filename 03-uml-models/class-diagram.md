# Class Diagram and Entity-Relationship Diagram

## OpenAI Enterprise Billing System — CST2310

---

## Class Diagram

```plantuml
@startuml
skinparam classAttributeIconSize 0

class User {
  - id: String
  - email: String
  - name: String
  - passwordHash: String
  - role: Role
  - departmentId: String
  - createdAt: DateTime
  - lastLogin: DateTime
  - isActive: Boolean
  + authenticate(email, password): Boolean
  + changePassword(oldPw, newPw): void
  + hasPermission(action): Boolean
}

enum Role {
  ADMINISTRATOR
  DEPARTMENT_MANAGER
  FINANCE_OFFICER
  DEVELOPER
  AUDITOR
}

class Department {
  - id: String
  - name: String
  - budget: Decimal
  - spent: Decimal
  - headcount: Integer
  - costCenter: String
  + getUtilisation(): Decimal
  + updateBudget(amount): void
  + addSpend(cost): void
  + isOverBudget(): Boolean
}

class Project {
  - id: String
  - name: String
  - departmentId: String
  - budget: Decimal
  - spent: Decimal
  - status: ProjectStatus
  - createdAt: DateTime
  + updateStatus(status): void
  + addSpend(cost): void
  + getRemainingBudget(): Decimal
}

enum ProjectStatus {
  ACTIVE
  PAUSED
  COMPLETED
}

class UsageRecord {
  - id: String
  - departmentId: String
  - model: String
  - inputTokens: Integer
  - outputTokens: Integer
  - totalTokens: Integer
  - cost: Decimal
  - date: DateTime
  - project: String
  - user: String
  + calculateCost(model): Decimal
}

class Model {
  - id: String
  - name: String
  - provider: String
  - costPer1kInput: Decimal
  - costPer1kOutput: Decimal
  - contextWindow: Integer
  - status: ModelStatus
  + calculateCost(inputTokens, outputTokens): Decimal
  + deprecate(): void
  + restrict(): void
}

enum ModelStatus {
  ACTIVE
  DEPRECATED
  RESTRICTED
}

class ApiKey {
  - id: String
  - name: String
  - keyHash: String
  - maskedKey: String
  - departmentId: String
  - createdAt: DateTime
  - lastUsed: DateTime
  - status: KeyStatus
  - permissions: List<String>
  + rotate(): ApiKey
  + revoke(): void
  + isExpired(): Boolean
  + getMaskedDisplay(): String
  + updateLastUsed(): void
}

enum KeyStatus {
  ACTIVE
  REVOKED
  EXPIRED
}

class Alert {
  - id: String
  - type: AlertType
  - severity: Severity
  - title: String
  - message: String
  - departmentId: String
  - createdAt: DateTime
  - acknowledged: Boolean
  - acknowledgedBy: String
  - acknowledgedAt: DateTime
  + acknowledge(userId): void
  + isEscalationDue(): Boolean
}

enum AlertType {
  QUOTA
  ANOMALY
  SECURITY
  BILLING
}

enum Severity {
  INFO
  WARNING
  CRITICAL
}

class AuditEntry {
  - id: String
  - action: String
  - actor: String
  - target: String
  - timestamp: DateTime
  - details: String
  - category: AuditCategory
}

enum AuditCategory {
  ACCESS
  CONFIG
  BILLING
  SECURITY
}

class AppSettings {
  - quotaThreshold: Integer
  - alertEmail: String
  - darkMode: Boolean
  - currency: Currency
  - role: Role
  - dataVersion: Integer
  + updateSetting(key, value): void
}

enum Currency {
  USD
  EUR
  GBP
}

class EnergyMetrics {
  - totalKwh: Decimal
  - co2Kg: Decimal
  - pueRatio: Decimal
  - renewablePercent: Decimal
  + compute(totalTokens): EnergyMetrics
}

' ── Relationships ──

User "1" --> "1" Role
User "0..*" --o "1" Department : belongs to

Department "1" *-- "0..*" Project : contains
Department "1" *-- "0..*" UsageRecord : has
Department "1" *-- "0..*" ApiKey : owns
Department "1" o-- "0..*" Alert : receives

Project "1" o-- "0..*" UsageRecord : tracks

UsageRecord "0..*" --> "1" Model : uses

ApiKey "1" --> "1" KeyStatus
ApiKey "0..*" --o "1" Department : assigned to

Alert "1" --> "1" AlertType
Alert "1" --> "1" Severity
Alert "0..1" --> "0..1" User : acknowledged by

AuditEntry "0..*" --> "1" AuditCategory
AuditEntry "0..*" --> "0..1" User : performed by

AppSettings "1" --> "1" Currency

@enduml
```

### Class Diagram Description

The class diagram models the core domain of the OpenAI Enterprise Billing system:

- **User** is the central actor entity, associated with a Role (enumeration) and optionally belonging to a Department (aggregation)
- **Department** is a composition parent for Projects, UsageRecords, and ApiKeys — when a department is deleted, its children are also removed
- **Project** tracks spend within a department and has a lifecycle status
- **UsageRecord** represents a single API usage event, associated with a Model for cost calculation
- **ApiKey** manages the credential lifecycle with statuses and permissions
- **Alert** is generated by the system and received by departments, with an acknowledgement workflow
- **AuditEntry** provides an immutable record of all significant system actions
- **EnergyMetrics** is a computed value object derived from total token usage

---

## Entity-Relationship Diagram (ERD)

```plantuml
@startuml
entity "User" as user {
  * user_id : VARCHAR(36) <<PK>>
  --
  * email : VARCHAR(255) <<UNIQUE>>
  * name : VARCHAR(100)
  * password_hash : VARCHAR(255)
  * role : ENUM('admin','dept_mgr','finance','developer','auditor')
  department_id : VARCHAR(36) <<FK>>
  * created_at : TIMESTAMP
  last_login : TIMESTAMP
  * is_active : BOOLEAN
}

entity "Department" as dept {
  * department_id : VARCHAR(36) <<PK>>
  --
  * name : VARCHAR(100)
  * budget : DECIMAL(12,2)
  * spent : DECIMAL(12,2)
  * headcount : INTEGER
  * cost_center : VARCHAR(20)
}

entity "Project" as proj {
  * project_id : VARCHAR(36) <<PK>>
  --
  * name : VARCHAR(100)
  * department_id : VARCHAR(36) <<FK>>
  * budget : DECIMAL(12,2)
  * spent : DECIMAL(12,2)
  * status : ENUM('active','paused','completed')
  * created_at : TIMESTAMP
}

entity "UsageRecord" as usage {
  * usage_id : VARCHAR(36) <<PK>>
  --
  * department_id : VARCHAR(36) <<FK>>
  * model_id : VARCHAR(36) <<FK>>
  * input_tokens : INTEGER
  * output_tokens : INTEGER
  * total_tokens : INTEGER
  * cost : DECIMAL(10,4)
  * date : TIMESTAMP
  project_id : VARCHAR(36) <<FK>>
  user_id : VARCHAR(36) <<FK>>
}

entity "Model" as model {
  * model_id : VARCHAR(36) <<PK>>
  --
  * name : VARCHAR(100)
  * provider : VARCHAR(50)
  * cost_per_1k_input : DECIMAL(8,6)
  * cost_per_1k_output : DECIMAL(8,6)
  * context_window : INTEGER
  * status : ENUM('active','deprecated','restricted')
}

entity "ApiKey" as apikey {
  * key_id : VARCHAR(36) <<PK>>
  --
  * name : VARCHAR(100)
  * key_hash : VARCHAR(255)
  * masked_key : VARCHAR(20)
  * department_id : VARCHAR(36) <<FK>>
  * created_at : TIMESTAMP
  last_used : TIMESTAMP
  * status : ENUM('active','revoked','expired')
  * permissions : JSON
}

entity "Alert" as alert {
  * alert_id : VARCHAR(36) <<PK>>
  --
  * type : ENUM('quota','anomaly','security','billing')
  * severity : ENUM('info','warning','critical')
  * title : VARCHAR(200)
  * message : TEXT
  department_id : VARCHAR(36) <<FK>>
  * created_at : TIMESTAMP
  * acknowledged : BOOLEAN
  acknowledged_by : VARCHAR(36) <<FK>>
  acknowledged_at : TIMESTAMP
}

entity "AuditEntry" as audit {
  * audit_id : VARCHAR(36) <<PK>>
  --
  * action : VARCHAR(100)
  * actor : VARCHAR(100)
  * target : VARCHAR(200)
  * timestamp : TIMESTAMP
  * details : TEXT
  * category : ENUM('access','config','billing','security')
}

entity "AppSettings" as settings {
  * setting_id : INTEGER <<PK>>
  --
  * quota_threshold : INTEGER
  * alert_email : VARCHAR(255)
  * dark_mode : BOOLEAN
  * currency : ENUM('USD','EUR','GBP')
  * data_version : INTEGER
}

' ── Relationships ──

user }o--|| dept : "belongs to"
proj }o--|| dept : "within"
usage }o--|| dept : "charged to"
usage }o--|| model : "uses"
usage }o--o| proj : "attributed to"
usage }o--o| user : "initiated by"
apikey }o--|| dept : "assigned to"
alert }o--o| dept : "relates to"
alert }o--o| user : "acknowledged by"

@enduml
```

### ERD Description

The ERD maps the domain model to a relational database schema:

- **Primary keys** use UUID (VARCHAR(36)) for all entities
- **Foreign keys** establish referential integrity between entities
- **Department** is the central entity: Users belong to departments, Projects exist within departments, UsageRecords are charged to departments, and ApiKeys are assigned to departments
- **UsageRecord** has foreign keys to Department (mandatory), Model (mandatory), Project (optional), and User (optional)
- **Alert** optionally relates to a Department and tracks acknowledgement by a User
- **AuditEntry** is intentionally denormalised (actor stored as string, not FK) to ensure audit entries remain meaningful even if the referenced user is deactivated
- **AppSettings** is a singleton table with a single row

### Cardinalities

| Relationship | Cardinality | Description |
|---|---|---|
| User → Department | Many-to-One | Many users can belong to one department |
| Department → Project | One-to-Many | A department can have many projects |
| Department → UsageRecord | One-to-Many | A department can have many usage records |
| Department → ApiKey | One-to-Many | A department can have many API keys |
| Model → UsageRecord | One-to-Many | A model can be referenced by many usage records |
| Project → UsageRecord | One-to-Many (optional) | A project can have many usage records; records may not specify a project |
| Department → Alert | One-to-Many (optional) | A department can receive many alerts; some alerts are system-wide |
| User → Alert | One-to-One (optional) | An alert can be acknowledged by one user |
